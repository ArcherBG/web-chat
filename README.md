This is a simple training project with React and Firebase.


## Available Scripts


In the project directory, you can run:

### `npm install` - Install dependencies

### `npm start` - Build and start project in dev mode
